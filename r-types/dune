(library
  (public_name rsem.r_types)
  (name r_types)
  (libraries mlsem rstt-repl)
)

(ocamllex lexer)

(menhir
 (modules parser)
 (explain true)
 ; (flags --table)
 )

(include_subdirs unqualified)
